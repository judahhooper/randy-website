<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Your Band</title>

    <!-- Bootstrap CSS -->
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" 
        rel="stylesheet"
    >

    <style>
        body {
            background-color: #111;
            color: #eee;
            font-family: system-ui, sans-serif;
        }

        header {
            padding: 3rem 1rem;
            text-align: center;
        }

        .band-title {
            font-size: 2.5rem;
            font-weight: bold;
        }

        .section-title {
            margin-top: 3rem;
            margin-bottom: 1rem;
            font-size: 1.5rem;
            font-weight: 600;
        }

        /* Email form block */
        .email-box {
            background: #1c1c1c;
            border-radius: 10px;
            padding: 1.5rem;
        }

        footer {
            margin-top: 3rem;
            padding: 1rem;
            text-align: center;
            color: #888;
        }
    </style>
</head>

<body>
    <!-- Header / Hero -->
    <header>
        <h1 class="band-title">YOUR BAND NAME</h1>
        <p class="lead">New music. Live shows. Loud riffs.</p>
    </header>

    <div class="container">

        <!-- Email Signup -->
        <h2 class="section-title">Join the Mailing List</h2>
        <div class="email-box">
            <p>Get show announcements and new music straight to your inbox.</p>

            <!-- Replace this block with Brevo embed code -->
            <form id="subscribe-form">
                <div class="mb-3">
                    <input 
                        type="email" 
                        class="form-control" 
                        id="emailInput" 
                        placeholder="you@example.com" 
                        required
                    >
                </div>
                <button type="submit" class="btn btn-primary w-100">
                    Subscribe
                </button>
                <small id="statusMessage" class="text-success d-none mt-2"></small>
            </form>

        </div>

        <!-- Shows -->
        <h2 class="section-title">Upcoming Shows</h2>
        <ul class="list-group">
            <li class="list-group-item bg-dark text-light">No shows announced yet</li>
        </ul>

        <!-- Social Links -->
        <h2 class="section-title">Follow Us</h2>
        <div class="d-flex gap-3">
            <a href="#" class="btn btn-outline-light btn-sm">Instagram</a>
            <a href="#" class="btn btn-outline-light btn-sm">Spotify</a>
            <a href="#" class="btn btn-outline-light btn-sm">YouTube</a>
        </div>
    </div>

    <footer>
        © 2025 Your Band Name
    </footer>

    <!-- Optional JS Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JS for handling the placeholder form (optional) -->
    <script>
        // This is only for the dummy form before you paste your Brevo form in.
        document.getElementById("subscribe-form").addEventListener("submit", function(e) {
            e.preventDefault();
            const status = document.getElementById("statusMessage");
            status.textContent = "Thanks! You'll hear from us soon.";
            status.classList.remove("d-none");
        });
    </script>

</body>
</html>
